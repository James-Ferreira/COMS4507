import React, { useEffect } from "react";

const AccessDenied = (props) => {
    return (
        <div>
            You must be an approved vet to access this page.  
        </div>
    );
};

export default AccessDenied;